# Businesses

<!-- BEGIN:GENERATED entity=Businesses -->

A **Business** represents an individual location or a group of locations in Nexudus. Most records in the system are connected — directly or via other records — to a business, which controls access and visibility based on the locations a user or customer can access.

Businesses can be organised into a hierarchy using `RootLocationId`, which references the parent business of a given business. A business with no `RootLocationId` is a top-level location. Child businesses inherit certain configuration from their parent.

Each business defines its own address, contact details, currency, opening hours, and public-facing content (Passport profile, website text, branding images). Read-only properties such as opening hours, theme, and image URLs reflect the current configuration set through the Nexudus admin UI.

Businesses support Search, Get, Update (no Create or Delete via API).

| Command | Description |
| --- | --- |
| `nexudus businesses list --agent` | List all businesses |
| `nexudus businesses list --id <id> --agent` | Filter by single ID |
| `nexudus businesses list --id <id1> --id <id2> --agent` | Filter by multiple IDs |
| `nexudus businesses list --unique-id <guid> --agent` | Filter by UniqueId (GUID) |
| `nexudus businesses list --name <value> --website <value> --agent` | Filter businesses by properties |
| `nexudus businesses list --page-number 2 --page-size 10 --agent` | Paginated list |
| `nexudus businesses get <id> --agent` | Get single business |
| `nexudus businesses update <id> --name "New Name" --agent` | Update business |

#### Business list filter options

`--visited-help-items` (enum), `--name`, `--default-language` (enum), `--space-website-language-id` (long), `--website`, `--default-payment-gateway-id` (long), `--terms`, `--short-intro`, `--about`, `--quote`, `--privacy-url`, `--cookie-url`, `--web-contact`, `--welcome-email`, `--address`, `--phone`, `--fax`, `--email`, `--trial-expire-date` (DateTime), `--from-trial-expire-date` (range), `--to-trial-expire-date` (range), `--last-over-due-invoice-reminder` (DateTime), `--from-last-over-due-invoice-reminder` (range), `--to-last-over-due-invoice-reminder` (range), `--last-welcome-email` (DateTime), `--from-last-welcome-email` (range), `--to-last-welcome-email` (range), `--country-id` (long), `--currency-id` (long), `--simple-time-zone-id` (long), `--new-logo-url`, `--clear-logo-file` (bool), `--new-banner-image-url`, `--clear-banner-image-file` (bool), `--new-nex-io-banner-image-url`, `--clear-nex-io-banner-image-file` (bool), `--discount-percentage` (decimal), `--from-discount-percentage` (range), `--to-discount-percentage` (range), `--discount-amount` (decimal), `--from-discount-amount` (range), `--to-discount-amount` (range), `--last-language-token-update` (DateTime), `--from-last-language-token-update` (range), `--to-last-language-token-update` (range), `--has-created-default-file-templates` (bool), `--invoicing-period` (int), `--from-invoicing-period` (range), `--to-invoicing-period` (range), `--suspended` (bool), `--custom-passport-app` (bool), `--reminders-processed-on` (DateTime), `--from-reminders-processed-on` (range), `--to-reminders-processed-on` (range), `--pre-auth-id`, `--last4`, `--preauth-error`, `--pre-auth-processed-on` (DateTime), `--from-pre-auth-processed-on` (range), `--to-pre-auth-processed-on` (range), `--last-charged-invoice` (int), `--from-last-charged-invoice` (range), `--to-last-charged-invoice` (range), `--last-charged-transaction` (int), `--from-last-charged-transaction` (range), `--to-last-charged-transaction` (range), `--last-charged-attendee` (int), `--from-last-charged-attendee` (range), `--to-last-charged-attendee` (range), `--passport-channels`, `--passport-only` (bool), `--passport-published` (bool), `--new-passport-banner-url`, `--clear-passport-banner-file` (bool), `--passport-name`, `--passport-tagline`, `--venue-type` (enum), `--tags`, `--floors` (int), `--from-floors` (range), `--to-floors` (range), `--floor-space` (int), `--from-floor-space` (range), `--to-floor-space` (range), `--floor-space-unit` (enum), `--longitude` (decimal), `--from-longitude` (range), `--to-longitude` (range), `--latitude` (decimal), `--from-latitude` (range), `--to-latitude` (range), `--passport-description`, `--city`, `--state`, `--postcode`, `--street-name`, `--street-number`, `--neighborhood`, `--contact-phone`, `--contact-email`, `--passport-members` (enum), `--passport-events` (enum), `--passport-community` (enum), `--passport-blog` (enum), `--mon-open` (int), `--from-mon-open` (range), `--to-mon-open` (range), `--mon-close` (int), `--from-mon-close` (range), `--to-mon-close` (range), `--tue-open` (int), `--from-tue-open` (range), `--to-tue-open` (range), `--tue-close` (int), `--from-tue-close` (range), `--to-tue-close` (range), `--wed-open` (int), `--from-wed-open` (range), `--to-wed-open` (range), `--wed-close` (int), `--from-wed-close` (range), `--to-wed-close` (range), `--thu-open` (int), `--from-thu-open` (range), `--to-thu-open` (range), `--thu-close` (int), `--from-thu-close` (range), `--to-thu-close` (range), `--fri-open` (int), `--from-fri-open` (range), `--to-fri-open` (range), `--fri-close` (int), `--from-fri-close` (range), `--to-fri-close` (range), `--sat-open` (int), `--from-sat-open` (range), `--to-sat-open` (range), `--sat-close` (int), `--from-sat-close` (range), `--to-sat-close` (range), `--sun-open` (int), `--from-sun-open` (range), `--to-sun-open` (range), `--sun-close` (int), `--from-sun-close` (range), `--to-sun-close` (range), `--mon-closed` (bool), `--tue-closed` (bool), `--wed-closed` (bool), `--thu-closed` (bool), `--fri-closed` (bool), `--sat-closed` (bool), `--sun-closed` (bool), `--same-opening` (bool), `--current-back-end-version` (int), `--from-current-back-end-version` (range), `--to-current-back-end-version` (range), `--authentication-provider`, `--created-by`, `--default-payment-term` (int), `--from-default-payment-term` (range), `--to-default-payment-term` (range), `--from-created-on` (range), `--to-created-on` (range), `--from-updated-on` (range), `--to-updated-on` (range)

#### Business update options

`--visited-help-items` (enum), `--name`, `--default-language` (enum), `--space-website-language-id` (long), `--website`, `--default-payment-gateway-id` (long), `--terms`, `--short-intro`, `--about`, `--quote`, `--privacy-url`, `--cookie-url`, `--web-contact`, `--welcome-email`, `--address`, `--phone`, `--fax`, `--email`, `--trial-expire-date` (DateTime), `--last-over-due-invoice-reminder` (DateTime), `--last-welcome-email` (DateTime), `--country-id` (long), `--currency-id` (long), `--simple-time-zone-id` (long), `--new-logo-url`, `--clear-logo-file` (bool), `--new-banner-image-url`, `--clear-banner-image-file` (bool), `--new-nex-io-banner-image-url`, `--clear-nex-io-banner-image-file` (bool), `--discount-percentage` (decimal), `--discount-amount` (decimal), `--last-language-token-update` (DateTime), `--has-created-default-file-templates` (bool), `--invoicing-period` (int), `--suspended` (bool), `--custom-passport-app` (bool), `--reminders-processed-on` (DateTime), `--pre-auth-id`, `--last4`, `--preauth-error`, `--pre-auth-processed-on` (DateTime), `--last-charged-invoice` (int), `--last-charged-transaction` (int), `--last-charged-attendee` (int), `--passport-channels`, `--passport-only` (bool), `--passport-published` (bool), `--new-passport-banner-url`, `--clear-passport-banner-file` (bool), `--passport-name`, `--passport-tagline`, `--venue-type` (enum), `--tags`, `--floors` (int), `--floor-space` (int), `--floor-space-unit` (enum), `--longitude` (decimal), `--latitude` (decimal), `--passport-description`, `--city`, `--state`, `--postcode`, `--street-name`, `--street-number`, `--neighborhood`, `--contact-phone`, `--contact-email`, `--passport-members` (enum), `--passport-events` (enum), `--passport-community` (enum), `--passport-blog` (enum), `--mon-open` (int), `--mon-close` (int), `--tue-open` (int), `--tue-close` (int), `--wed-open` (int), `--wed-close` (int), `--thu-open` (int), `--thu-close` (int), `--fri-open` (int), `--fri-close` (int), `--sat-open` (int), `--sat-close` (int), `--sun-open` (int), `--sun-close` (int), `--mon-closed` (bool), `--tue-closed` (bool), `--wed-closed` (bool), `--thu-closed` (bool), `--fri-closed` (bool), `--sat-closed` (bool), `--sun-closed` (bool), `--same-opening` (bool), `--current-back-end-version` (int), `--authentication-provider`, `--created-by`, `--default-payment-term` (int)

### Business (key fields)

`Id`, `Name`, `WebAddress`, `Address`, `Phone`, `EmailContact`, `CountryName`, `CurrencyCode`, `PassportPublished`, `VenueType`, `Tags`, `TownCity`, `State`, `PostalCode`

<!-- END:GENERATED entity=Businesses -->
