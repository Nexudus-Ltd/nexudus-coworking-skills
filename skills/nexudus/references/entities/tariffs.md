# Tariffs

<!-- BEGIN:GENERATED entity=Tariffs -->

Tariffs support Search, Get, Create, Update, Delete.

| Command | Description |
| --- | --- |
| `nexudus tariffs list --agent` | List all tariffs |
| `nexudus tariffs list --id <id> --agent` | Filter by single ID |
| `nexudus tariffs list --id <id1> --id <id2> --agent` | Filter by multiple IDs |
| `nexudus tariffs list --unique-id <guid> --agent` | Filter by UniqueId (GUID) |
| `nexudus tariffs list --name <value> --system-tariff-type <value> --agent` | Filter tariffs by properties |
| `nexudus tariffs list --page-number 2 --page-size 10 --agent` | Paginated list |
| `nexudus tariffs get <id> --agent` | Get single tariff |
| `nexudus tariffs create --business-id <value> --name <value> --system-tariff-type <value> --price <value> --currency-id <value> --cancellation-period <value> --display-order <value> --invoice-every <value> --invoice-every-weeks <value> --booking-due-date-strategy <value> --address-identity-check-provider <value> --address-identity-check-repeat-pattern <value> --identity-check-provider <value> --identity-check-repeat-pattern <value> --agent` | Create tariff |
| `nexudus tariffs update <id> --name "New Name" --agent` | Update tariff |
| `nexudus tariffs delete <id> --yes --agent` | Delete tariff (no prompt) |

#### Tariff list filter options

`--business-id` (long), `--name`, `--system-tariff-type` (enum), `--price` (decimal), `--from-price` (range), `--to-price` (range), `--default-invoicing-day` (int), `--from-default-invoicing-day` (range), `--to-default-invoicing-day` (range), `--visible` (bool), `--use-time-passes` (bool), `--description`, `--invoice-line-display-as`, `--sign-up-fee` (decimal), `--from-sign-up-fee` (range), `--to-sign-up-fee` (range), `--currency-id` (long), `--tax-rate-id` (long), `--reduced-tax-rate-id` (long), `--exempt-tax-rate-id` (long), `--financial-account-id` (long), `--terms-and-conditions`, `--new-contract-document-url`, `--clear-contract-document-file` (bool), `--cancellation-period` (int), `--from-cancellation-period` (range), `--to-cancellation-period` (range), `--display-order` (int), `--from-display-order` (range), `--to-display-order` (range), `--group-name`, `--disable-portal-cancellations` (bool), `--subscribers-limit` (int), `--from-subscribers-limit` (range), `--to-subscribers-limit` (range), `--cancellation-limit-days` (int), `--from-cancellation-limit-days` (range), `--to-cancellation-limit-days` (range), `--default-contract-term` (int), `--from-default-contract-term` (range), `--to-default-contract-term` (range), `--cancel-member-account-after` (int), `--from-cancel-member-account-after` (range), `--to-cancel-member-account-after` (range), `--checkin-price-plan-limit` (int), `--from-checkin-price-plan-limit` (range), `--to-checkin-price-plan-limit` (range), `--checkin-month-limit` (int), `--from-checkin-month-limit` (range), `--to-checkin-month-limit` (range), `--checkin-week-limit` (int), `--from-checkin-week-limit` (range), `--to-checkin-week-limit` (range), `--visitor-month-limit` (int), `--from-visitor-month-limit` (range), `--to-visitor-month-limit` (range), `--visitor-week-limit` (int), `--from-visitor-week-limit` (range), `--to-visitor-week-limit` (range), `--visitor-day-limit` (int), `--from-visitor-day-limit` (range), `--to-visitor-day-limit` (range), `--hours-price-plan-limit` (int), `--from-hours-price-plan-limit` (range), `--to-hours-price-plan-limit` (range), `--hours-month-limit` (int), `--from-hours-month-limit` (range), `--to-hours-month-limit` (range), `--hours-week-limit` (int), `--from-hours-week-limit` (range), `--to-hours-week-limit` (range), `--booking-minute-week-limit` (int), `--from-booking-minute-week-limit` (range), `--to-booking-minute-week-limit` (range), `--booking-minute-month-limit` (int), `--from-booking-minute-month-limit` (range), `--to-booking-minute-month-limit` (range), `--discount-extra-services` (decimal), `--from-discount-extra-services` (range), `--to-discount-extra-services` (range), `--discount-time-passes` (decimal), `--from-discount-time-passes` (range), `--to-discount-time-passes` (range), `--discount-charges` (decimal), `--from-discount-charges` (range), `--to-discount-charges` (range), `--invoice-every` (int), `--from-invoice-every` (range), `--to-invoice-every` (range), `--invoice-every-weeks` (int), `--from-invoice-every-weeks` (range), `--to-invoice-every-weeks` (range), `--auto-cancel-after` (int), `--from-auto-cancel-after` (range), `--to-auto-cancel-after` (range), `--advance-invoice-cycles` (int), `--from-advance-invoice-cycles` (range), `--to-advance-invoice-cycles` (range), `--prorate-day-of-month` (int), `--from-prorate-day-of-month` (range), `--to-prorate-day-of-month` (range), `--prorate-days-before` (int), `--from-prorate-days-before` (range), `--to-prorate-days-before` (range), `--prorate-cancellations` (bool), `--charge-and-extend` (int), `--from-charge-and-extend` (range), `--to-charge-and-extend` (range), `--exclude-from-invoice` (bool), `--auto-raise-invoices` (bool), `--raise-invoice-every` (int), `--from-raise-invoice-every` (range), `--to-raise-invoice-every` (range), `--raise-invoice-every-weeks` (int), `--from-raise-invoice-every-weeks` (range), `--to-raise-invoice-every-weeks` (range), `--minimum-price` (decimal), `--from-minimum-price` (range), `--to-minimum-price` (range), `--minimum-price-include-time-passes` (bool), `--minimum-price-include-extra-services` (bool), `--minimum-price-include-events` (bool), `--archived` (bool), `--starred` (bool), `--keep-new-accounts-on-hold` (bool), `--can-be-paused` (bool), `--pause-yearly-limit` (int), `--from-pause-yearly-limit` (range), `--to-pause-yearly-limit` (range), `--pause-cycles-limit` (int), `--from-pause-cycles-limit` (range), `--to-pause-cycles-limit` (range), `--booking-due-date-strategy` (enum), `--booking-due-date-day-of-month` (int), `--from-booking-due-date-day-of-month` (range), `--to-booking-due-date-day-of-month` (range), `--is-virtual-office` (bool), `--request-address-identity-check` (bool), `--address-identity-check-description`, `--address-identity-check-provider` (enum), `--keep-paused-if-address-mismatch` (bool), `--address-identity-check-repeat-pattern` (enum), `--request-identity-check` (bool), `--identity-check-provider` (enum), `--identity-check-repeat-pattern` (enum), `--identity-check-description`, `--send-onboarding-form-by-email` (bool), `--form-page-id` (long), `--delivery-preferences-mail`, `--delivery-preferences-parcels`, `--delivery-preferences-checks`, `--delivery-preferences-publicity`, `--delivery-preferences-other`, `--maximum-delivery-storage-days` (int), `--from-maximum-delivery-storage-days` (range), `--to-maximum-delivery-storage-days` (range), `--maximum-company-aliases` (int), `--from-maximum-company-aliases` (range), `--to-maximum-company-aliases` (range), `--maximum-recipients` (int), `--from-maximum-recipients` (range), `--to-maximum-recipients` (range), `--maximum-addresses` (int), `--from-maximum-addresses` (range), `--to-maximum-addresses` (range), `--transfer-products-to-contract` (bool), `--from-created-on` (range), `--to-created-on` (range), `--from-updated-on` (range), `--to-updated-on` (range)

#### Tariff create options

`--business-id` (long, required), `--name` (required), `--system-tariff-type` (enum, required), `--price` (decimal, required), `--default-invoicing-day` (int), `--visible` (bool), `--use-time-passes` (bool), `--description`, `--invoice-line-display-as`, `--sign-up-fee` (decimal), `--currency-id` (long, required), `--tax-rate-id` (long), `--reduced-tax-rate-id` (long), `--exempt-tax-rate-id` (long), `--financial-account-id` (long), `--terms-and-conditions`, `--new-contract-document-url`, `--clear-contract-document-file` (bool), `--cancellation-period` (int, required), `--display-order` (int, required), `--group-name`, `--disable-portal-cancellations` (bool), `--subscribers-limit` (int), `--cancellation-limit-days` (int), `--default-contract-term` (int), `--cancel-member-account-after` (int), `--checkin-price-plan-limit` (int), `--checkin-month-limit` (int), `--checkin-week-limit` (int), `--visitor-month-limit` (int), `--visitor-week-limit` (int), `--visitor-day-limit` (int), `--hours-price-plan-limit` (int), `--hours-month-limit` (int), `--hours-week-limit` (int), `--booking-minute-week-limit` (int), `--booking-minute-month-limit` (int), `--discount-extra-services` (decimal), `--discount-time-passes` (decimal), `--discount-charges` (decimal), `--invoice-every` (int, required), `--invoice-every-weeks` (int, required), `--auto-cancel-after` (int), `--advance-invoice-cycles` (int), `--prorate-day-of-month` (int), `--prorate-days-before` (int), `--prorate-cancellations` (bool), `--charge-and-extend` (int), `--exclude-from-invoice` (bool), `--auto-raise-invoices` (bool), `--raise-invoice-every` (int), `--raise-invoice-every-weeks` (int), `--minimum-price` (decimal), `--minimum-price-include-time-passes` (bool), `--minimum-price-include-extra-services` (bool), `--minimum-price-include-events` (bool), `--archived` (bool), `--starred` (bool), `--keep-new-accounts-on-hold` (bool), `--can-be-paused` (bool), `--pause-yearly-limit` (int), `--pause-cycles-limit` (int), `--booking-due-date-strategy` (enum, required), `--booking-due-date-day-of-month` (int), `--is-virtual-office` (bool), `--request-address-identity-check` (bool), `--address-identity-check-description`, `--address-identity-check-provider` (enum, required), `--keep-paused-if-address-mismatch` (bool), `--address-identity-check-repeat-pattern` (enum, required), `--request-identity-check` (bool), `--identity-check-provider` (enum, required), `--identity-check-repeat-pattern` (enum, required), `--identity-check-description`, `--send-onboarding-form-by-email` (bool), `--form-page-id` (long), `--products-store` (list, repeat flag), `--added-products-store` (list, repeat flag), `--removed-products-store` (list, repeat flag), `--products-forward` (list, repeat flag), `--added-products-forward` (list, repeat flag), `--removed-products-forward` (list, repeat flag), `--products-recycle` (list, repeat flag), `--added-products-recycle` (list, repeat flag), `--removed-products-recycle` (list, repeat flag), `--products-shred` (list, repeat flag), `--added-products-shred` (list, repeat flag), `--removed-products-shred` (list, repeat flag), `--products-scan` (list, repeat flag), `--added-products-scan` (list, repeat flag), `--removed-products-scan` (list, repeat flag), `--products-return` (list, repeat flag), `--added-products-return` (list, repeat flag), `--removed-products-return` (list, repeat flag), `--products-deposit` (list, repeat flag), `--added-products-deposit` (list, repeat flag), `--removed-products-deposit` (list, repeat flag), `--products-collect` (list, repeat flag), `--added-products-collect` (list, repeat flag), `--removed-products-collect` (list, repeat flag), `--delivery-preferences-mail`, `--delivery-preferences-parcels`, `--delivery-preferences-checks`, `--delivery-preferences-publicity`, `--delivery-preferences-other`, `--maximum-delivery-storage-days` (int), `--maximum-company-aliases` (int), `--maximum-recipients` (int), `--maximum-addresses` (int), `--transfer-products-to-contract` (bool)

#### Tariff update options

`--business-id` (long), `--name`, `--system-tariff-type` (enum), `--price` (decimal), `--default-invoicing-day` (int), `--visible` (bool), `--use-time-passes` (bool), `--description`, `--invoice-line-display-as`, `--sign-up-fee` (decimal), `--currency-id` (long), `--tax-rate-id` (long), `--reduced-tax-rate-id` (long), `--exempt-tax-rate-id` (long), `--financial-account-id` (long), `--terms-and-conditions`, `--new-contract-document-url`, `--clear-contract-document-file` (bool), `--cancellation-period` (int), `--display-order` (int), `--group-name`, `--disable-portal-cancellations` (bool), `--subscribers-limit` (int), `--cancellation-limit-days` (int), `--default-contract-term` (int), `--cancel-member-account-after` (int), `--checkin-price-plan-limit` (int), `--checkin-month-limit` (int), `--checkin-week-limit` (int), `--visitor-month-limit` (int), `--visitor-week-limit` (int), `--visitor-day-limit` (int), `--hours-price-plan-limit` (int), `--hours-month-limit` (int), `--hours-week-limit` (int), `--booking-minute-week-limit` (int), `--booking-minute-month-limit` (int), `--discount-extra-services` (decimal), `--discount-time-passes` (decimal), `--discount-charges` (decimal), `--invoice-every` (int), `--invoice-every-weeks` (int), `--auto-cancel-after` (int), `--advance-invoice-cycles` (int), `--prorate-day-of-month` (int), `--prorate-days-before` (int), `--prorate-cancellations` (bool), `--charge-and-extend` (int), `--exclude-from-invoice` (bool), `--auto-raise-invoices` (bool), `--raise-invoice-every` (int), `--raise-invoice-every-weeks` (int), `--minimum-price` (decimal), `--minimum-price-include-time-passes` (bool), `--minimum-price-include-extra-services` (bool), `--minimum-price-include-events` (bool), `--archived` (bool), `--starred` (bool), `--keep-new-accounts-on-hold` (bool), `--can-be-paused` (bool), `--pause-yearly-limit` (int), `--pause-cycles-limit` (int), `--booking-due-date-strategy` (enum), `--booking-due-date-day-of-month` (int), `--is-virtual-office` (bool), `--request-address-identity-check` (bool), `--address-identity-check-description`, `--address-identity-check-provider` (enum), `--keep-paused-if-address-mismatch` (bool), `--address-identity-check-repeat-pattern` (enum), `--request-identity-check` (bool), `--identity-check-provider` (enum), `--identity-check-repeat-pattern` (enum), `--identity-check-description`, `--send-onboarding-form-by-email` (bool), `--form-page-id` (long), `--products-store` (list, repeat flag), `--added-products-store` (list, repeat flag), `--removed-products-store` (list, repeat flag), `--products-forward` (list, repeat flag), `--added-products-forward` (list, repeat flag), `--removed-products-forward` (list, repeat flag), `--products-recycle` (list, repeat flag), `--added-products-recycle` (list, repeat flag), `--removed-products-recycle` (list, repeat flag), `--products-shred` (list, repeat flag), `--added-products-shred` (list, repeat flag), `--removed-products-shred` (list, repeat flag), `--products-scan` (list, repeat flag), `--added-products-scan` (list, repeat flag), `--removed-products-scan` (list, repeat flag), `--products-return` (list, repeat flag), `--added-products-return` (list, repeat flag), `--removed-products-return` (list, repeat flag), `--products-deposit` (list, repeat flag), `--added-products-deposit` (list, repeat flag), `--removed-products-deposit` (list, repeat flag), `--products-collect` (list, repeat flag), `--added-products-collect` (list, repeat flag), `--removed-products-collect` (list, repeat flag), `--delivery-preferences-mail`, `--delivery-preferences-parcels`, `--delivery-preferences-checks`, `--delivery-preferences-publicity`, `--delivery-preferences-other`, `--maximum-delivery-storage-days` (int), `--maximum-company-aliases` (int), `--maximum-recipients` (int), `--maximum-addresses` (int), `--transfer-products-to-contract` (bool)

### Tariff (key fields)

`Id`, `Name`, `SystemTariffType`, `Price`, `Visible`, `Archived`

**List properties (only returned by `get`, not by `list`):** `ProductsStore`, `AddedProductsStore`, `RemovedProductsStore`, `ProductsForward`, `AddedProductsForward`, `RemovedProductsForward`, `ProductsRecycle`, `AddedProductsRecycle`, `RemovedProductsRecycle`, `ProductsShred`, `AddedProductsShred`, `RemovedProductsShred`, `ProductsScan`, `AddedProductsScan`, `RemovedProductsScan`, `ProductsReturn`, `AddedProductsReturn`, `RemovedProductsReturn`, `ProductsDeposit`, `AddedProductsDeposit`, `RemovedProductsDeposit`, `ProductsCollect`, `AddedProductsCollect`, `RemovedProductsCollect`

#### Tariff enum values

| Option | Valid values |
| ------ | ------------ |
| `--system-tariff-type` | `1` FullTimePrivateOffice, `2` PartTimePrivateOffice, `3` FullTimeDedicatedDesk, `4` PartTimeDedicatedDesk, `5` FullTimeHotDesk, `6` PartTimeHotDesk, `7` FullTimeOther, `8` PartTimeOther, `9` Storage, `10` VirtualOffice, `11` Virtual, `99` Other |
| `--booking-due-date-strategy` | `1` RenewalDate, `2` BookingEndDate, `3` BookingCreationDate, `4` NextNthOfMonth |
| `--address-identity-check-provider` | `1` Manual, `2` StripeIdentity |
| `--address-identity-check-repeat-pattern` | `1` Never, `2` Every3Months, `3` Every6Months, `4` Every12Months, `5` Every24Months |
| `--identity-check-provider` | `1` Manual, `2` StripeIdentity |
| `--identity-check-repeat-pattern` | `1` Never, `2` Every3Months, `3` Every6Months, `4` Every12Months, `5` Every24Months |

<!-- END:GENERATED entity=Tariffs -->
